<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.uds.pautando">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/pautando_ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/pautando_ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".features.create_meeting_agenda.presentation.pages.CreateMeetingAgendaPage"></activity>
        <activity android:name=".features.forgot_password.presentation.pages.ForgotPasswordPage" />
        <activity android:name=".features.sign_up.presentation.pages.SignUpPage" />
        <activity android:name=".features.home.presentation.pages.HomePage" />
        <activity android:name=".features.sign_in.presentation.pages.SignInPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>